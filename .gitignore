node_modules/
_site/
.DS_Store

# Generated by scripts/make-music.mjs. Deterministic - a seeded LCG and no
# Math.random - so it reproduces byte for byte from the script and is not worth
# 11MB of history.
public/explainer-music.wav

# The explainer render. It is served from the blog repository, where it is
# committed; this repo only needs the composition that produces it.
media/amordle-explainer.mp4
media/amordle-explainer-poster.png
